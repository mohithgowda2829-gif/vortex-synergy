spring:
  jpa:
    hibernate:
      # Keep update for this academic deployment because the project does not
      # yet use versioned Flyway/Liquibase migrations.
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false

app:
  security:
    expose-password-reset-token: ${EXPOSE_PASSWORD_RESET_TOKEN:false}
